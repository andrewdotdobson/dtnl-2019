import { Link } from "gatsby";
import PropTypes from "prop-types";
import React, { useState } from "react";

function Footer({}) {

  return (
    <div
      className="flex flex-wrap items-left max-w-xl"
      >
      <p>Digitonal</p>
      </div>
  );
}



export default Footer;
